import Header from "../Header";
import { StyledFooter } from "./Footer.styles";

export default function Footer() {
    return (
        <StyledFooter>
            <Header />
        </StyledFooter>
    );
}
