import { StyledButton } from "./BotaoContato.styles";

export default function BotaoContato() {
    return (
        <StyledButton href="#contato">
            Contato
        </StyledButton>
    );
}
